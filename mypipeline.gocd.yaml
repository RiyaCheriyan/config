format_version: 5
pipelines:
   new:
      group:
      materials:
         mygit:
           git: file:///var/go/tmp/infra-as-code/.git/
      stages:
       plan:
         clean_workspace: true
         approval: auto
         jobs:
          TerraPlan:
           tasks:
           -script:
              pwd
              terraform init
              terraform plan
