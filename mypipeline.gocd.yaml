format_version: 5
pipelines:
  new:
    group: demo
    materials:
      mygit:
        git: file:///var/go/tmp/infra-as-code/.git/
        branch: master
    stages:
     - build:
         jobs:
           build:
             tasks:
              - exec:
                  command: pwd
                  command: terraform init
    stage2:
     - build1:
         jobs:
           helo: 
             tasks:
              - exec:
                  command: env
             
